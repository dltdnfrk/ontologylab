# 온톨로지랩 시운전 버그리포트 재검수 — 2026-08-05

- **검수 시각**: 2026-08-05 07:20–07:40 KST
- **검수 대상 문서**: `ontologylab/docs/ONTOLOGYLAB-SMOKE-TEST-KO.md` (시운전 20:58–21:22)
- **재검증 방법**: 라이브 UI(DOM 계측) + 코드 확인 + git 상태 대조
- **라이브 상태**: `http://127.0.0.1:8799/`, 문서 29 · 대기 327 · 승인 0 · 팩 1
- **커밋 상태**: 수정사항 전부 **미커밋 작업 트리** (chatstore.py·routes.py·app.js·index.html 수정, chat-session.js·localize.js 등 untracked)

## 0. 총평

스모크 리포트의 P1/P2/P3(홈 세션·그래프·제목 태그)는 재검수에서도 유효하고, 리포트 자체가 수정 후 재검수를 상세히 기록했다.  
그러나 **검토 큐 성능 결함(327건 전체 렌더)이 리포트에 완전히 빠져 있다.** 이것이 이번 검수의 최대 누락이다.  
그 외 실행 이력 영속성, 아티팩트 라이브러리 부재는 리포트 범위(6개 시나리오)에 없었던 영역이다.

---

## 1. 기존 결함 재검증 상태

| ID | 제목 | 심각도 | 리포트 상태 | 재검수 상태 | 근거 |
|---|---|---|---|---|---|
| P1 | 홈 진입 시 이전 대화 잔존 | High | 수정 확인 | **유지(수정)** | `chat-session.js` sessionStorage 고유 session_id + `startsNewOnEntry` |
| P2 | 그래프 라벨 겹침·영문 잔존 | High | 수정 확인 | **유지(수정)** | 리포트 내 재검수(14개 라벨, 겹침 0, 말줄임 0) |
| P3 | 논문 제목 HTML 태그 노출 | Medium | 수정 확인 | **유지(수정)** | 리포트 내 재검수(29개 문서, 태그 노출 0, a11y 이름 일치) |

## 2. 리포트에서 빠진 항목 (GAP)

### GAP-O1 (High) — 검토 큐 327건 전체 렌더 (DOM 4,231 노드 · 버튼 451개)

- 라이브 실측 (2026-08-05 07:22):
  - `#proposals-body`에 **200행 렌더** (API limit=200, `app.js:694` 하드코드)
  - 버튼 451개 · 체크박스 200개 · 전체 DOM 4,231 노드 · main scrollHeight 7,818px
  - 대기 327건일 때 한 화면에 200건을 전부 DOM으로 생성
- 리포트 미기재: 시나리오 4 "검토 기능"을 **합격**으로 판정. 합격 기준이 "대기 건수 즉시 변경"뿐이라 성능/확장성을 아예 평가하지 않음.
- 영향: 500+ 제안 시 DOM 1만+ 노드, 키보드 검토·근거 패널 갱신 지연. CS 이식 명세 §4.5·§6.1(P0 가상화)과 직결.
- 재현: 대기 327건 상태에서 검토 탭 진입.

### GAP-O2 (Medium) — 실행 이력(Job)이 메모리 영속, 서버 재시작 시 목록 소실

- 코드 확인: `JobRegistry`가 in-memory (`server/jobs.py`). 서버 재시작 시 jobs 목록 UI 공백 (홈 화면 주석도 인정).
- 시나리오 5/6이 "웹앱 정상 표시/자동 복구"만 확인하고 **실행 이력 보존은 미검증**.
- 영향: 추출/리서치 이력·비용 요약이 리셋. CS 이식 명세 §7(durable Run)과 직결.

### GAP-O3 (Medium) — 팩·중간 산출물을 여는 아티팩트 라이브러리 부재 (검증 공백)

- 시나리오에는 팩 화면 "열기"만 있고(시나리오 2 메뉴 확인), **팩 상세·원문·추출 JSON·그림을 열람/버전 비교하는 항목이 없음**.
- 현재 UI는 문서 패널(doc-panel)과 팩 목록만 존재. 원문 29건·제안 327건이지만 "검토 후 팩" 외의 산출물 소비 경로가 없음.
- 리포트 미기재: 버그라기보다 **검증 커버리지 공백**이지만, 시나리오 6개에 "산출물 열람" 항목을 추가해야 함.

### GAP-O4 (Low) — 리서치 시 소스 3개 fetch_failed의 사용자 표면화 미검증

- 리포트가 "Crossref·Semantic Scholar·SearXNG는 fetch_failed"로 기록했으나, **실패 원인·재시도·사용자 안내가 화면에 충분히 드러나는지**는 검증 항목이 없음.
- 영향: 소스별 장애가 조용히 흡수되면 "7개 중 4개만 응답"이 사용자에게 안 보임.

### GAP-O5 (Info) — 수정사항 전부 미커밋

- `git status`: P1/P2/P3 수정 + 테스트 파일들이 전부 미커밋/untracked. 스모크 리포트는 "아직 커밋되지 않은 작업 트리 기준"으로 정직하게 명시했으나, **커밋 시점·브랜치 정책이 없음**.
- 추가: `web/index 2.html` 파일 잔존 (정리 대상).

## 3. 리포트에서 확인된 것 (누락 아님, 유지)

- P1 재검수(홈 진입 → 새 session_id, 입력값 초기화, 저장소 수치 유지) 정확.
- P2/P3 수정 후 격리 Chromium 재검수(0 overlap, 0 ellipsis, 29개 a11y 일치) 정확.
- 1/5/6 시나리오의 LaunchServices/launchd 제한 판정은 이 샌드박스 한정으로 정확.
- 38→40개 집중 검사, 전체 1637 통과 기록 유효.

## 4. 시나리오 커버리지

| # | 시나리오 | 시운전 판정 | 재검수 판정 |
|---|---|---|---|
| 1 | 앱 실행 | 검증 제한 | 사용자 환경 재확인 필요 |
| 2 | 한국어 화면 | 실패 → 수정 | **유지(수정 확인)** |
| 3 | 리서치 실행 | 부분 합격 | 유지 (GAP-O4 보완 권장) |
| 4 | 검토 기능 | 합격 | **GAP-O1 미반영 — 성능 기준 추가 후 재판정 필요** |
| 5 | 재실행과 흔적 | 검증 제한 | 사용자 환경 재확인 + GAP-O2 검증 추가 |
| 6 | 오류 복구 | 부분 확인 | 사용자 환경 재확인 |

## 5. 권고 우선순위 (기존 리포트 + GAP 반영)

1. **P0** GAP-O1: 검토 큐 가상화/페이지네이션 (327건 전체 렌더 제거) — 라이브에서 즉시 재현되는 최대 누락
2. **P0** 시나리오 4 합격 기준에 성능 상한 추가 (예: 초기 렌더 행 ≤ 100)
3. **P1** GAP-O2: Job → durable Run 영속화 (서버 재시작 후 이력 유지)
4. **P1** GAP-O3: Artifacts 라이브러리(원문/추출/팩) 시나리오 추가
5. **P2** GAP-O4: 소스별 fetch_failed 사용자 표면화
6. **Ops** GAP-O5: 수정사항 커밋 + `web/index 2.html` 정리
7. **확인** 사용자 환경: 시나리오 1/5/6 (앱 아이콘·Stop·재실행)

## 6. 증거 인덱스

| 주장 | 근거 |
|---|---|
| 200행/451버튼/4,231 DOM | 라이브 DOM 계측 (2026-08-05 07:22) |
| limit=200 하드코드 | `web/app.js:694` |
| Job 메모리 영속 | `server/jobs.py` JobRegistry in-memory |
| P1/P2/P3 수정 유지 | `chat-session.js` + 스모크 리포트 내 재검수 + 라이브 |
| 미커밋 상태 | git status (수정 + untracked 다수) |

*이 문서는 시운전 리포트의 누락을 채우는 검수본이다. 원본 리포트와 함께 보관.*

---

## 부록 — GAP-O1 수정 기록 (2026-08-05)

- **상태**: 수정 확인 (미커밋 작업 트리)
- **수정**: 검토 큐를 keyset 커서 페이지네이션으로 전환.
  - API `GET /api/proposals` — `limit` 기본 50·상한 100, `cursor`·`has_more`·`next_cursor` 추가 (`ontologylab/server/routes.py`)
  - `KGStore.pending_review` — keyset 커서 지원, `pr.id` tiebreak로 전 순서 안정화 (`ontologylab/kgstore.py`)
  - UI — 첫 페이지 50행 렌더, 스크롤/`j`/`k`로 추가 로드 (`web/app.js`)
- **재검증**: `tests/test_proposals_pagination.py` 6건 — 커서 안정 순서·중복/누락 없음·기본 50·상한 100 강제·불량 커서 400·UI 첫 렌더 ≤ 100행.
- **수락 기준**(DoD) 반영: 시운전 시나리오 4 합격 기준에 “초기 렌더 행 ≤ 100” 추가.
- **잔존**: GAP-O2(실행 이력 영속), GAP-O3(아티팩트 라이브러리), GAP-O4(소스 실패 표면화)는 P1/P2로 미해결.
