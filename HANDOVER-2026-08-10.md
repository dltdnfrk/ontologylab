# OntologyLab Roadmap — 작업 인수인계서 (갱신본)

> 작성일: 2026-08-10  
> 목적: 다음 세션이 남은 마감 작업만 처리하고 goal을 닫을 수 있도록 현재 디스크 상태를 기준으로 전달  
> 이전 요약의 "lost commits / 거의 완료" 서술은 **무시**하고 이 문서의 디스크 상태를 권위로 삼을 것

---

## 1. 한 줄 상태

**P0–P5 실행 작업은 커밋까지 완료. 계획서 체크박스 1–10 + F1–F4 모두 `[x]`.  
남은 것은 최종 스위트 재확인, Boulder/final-verdict 아티팩트 생성, todo 정리, `update_goal(complete)` 뿐이다.**

---

## 2. 프로젝트 경계

| 항목 | 값 |
|---|---|
| Canonical root | `/Users/hyunjun/Documents/MUNI/ontologylab` |
| Git remote | `origin https://github.com/dltdnfrk/ontologylab.git` |
| Branch | `main` |
| HEAD | `66481c6` |
| origin/main | `4ee5465` (로컬이 13 커밋 앞섬, **push 안 함**) |
| Plan | `.omo/plans/ontology-platform-roadmap.md` |
| Ledger | `.omo/start-work/ledger.jsonl` |
| Evidence | `.omo/evidence/ontology-platform-roadmap/` |
| Boulder | **없음** (`.sisyphus/boulder.json` 미생성) |
| Final verdict | **없음** |

### 불변량 (절대 깨지 말 것)
- AI proposes / human decides
- verified-only packs
- read-only MCP
- local-first / single-user
- web QA = Aside/Orca computer-use (Playwright/Selenium 금지)
- 실제 데이터 경로: `~/Library/Application Support/ontologylab/data` — QA는 항상 mktemp
- 테스트: `PYTHONPATH=. .venv/bin/pytest` (PYTHONPATH 필수)
- 파이썬: `.venv/bin/python` (system python3=3.8 금지)
- FastAPI route 직접 호출 금지 — TestClient only
- 형제 프로젝트(MUNI LAB 등) 접근 금지

---

## 3. 커밋 히스토리 (실제 디스크)

```
66481c6 (HEAD) test(acceptance): Aside browser evidence for semantic journeys   ← P5-A
7de03f0          feat(evaluation): frozen competency release gates               ← P2-A
6c74b83          feat(ontology): add verified extraction proposal workflow       ← P1-D (추가)
71e93f3          feat(pack): verified and license-gated ontology publication     ← P1-C
b2b8548          feat(web): ontology term lifecycle and xref review workflow     ← P1-B
56d39af          feat(ontology): stable term identity, lifecycle, typed xref     ← P1-A
6c0b127          feat(pack): version-coherent multi-schema pack contract         ← P0-D
3878488          fix(pack): align migrated edges column order
0b88444          feat(schema): first-class relation qualifiers                   ← P0-C
6b59c65          fix(kgstore): keep legacy-relation edges visible
b1c601c          chore(product-status): refresh evidence digests
756b010          fix(kgstore): enforce schema-boundary validation                ← P0-A
87d097b          fix(mcp): verify pack content hash at load time                 ← P0-B
4ee5465 (origin) Ignore agent-run working artifacts (.omo)
```

### 이전에 "손실"로 보고됐던 커밋
`3242795`, `b0fb7e0`, `d0868f5`, `5d2089c`, `83174a7`, `7cdca47`, `80ef2f2` — **객체 없음, 재실행 불필요**.  
이번 세션에서 P2-A / P3-A(no-go) / P5-A를 실제 커밋으로 다시 닫았다.

---

## 4. 계획서 체크박스 (전부 닫힘)

`.omo/plans/ontology-platform-roadmap.md`:

| # | 작업 | 상태 | 커밋 / 비고 |
|---|---|---|---|
| 1 | P0-A schema boundary + direction | [x] | `756b010` |
| 2 | P0-B pack hash verify | [x] | `87d097b` |
| 3 | P0-C relation qualifiers | [x] | `0b88444` |
| 4 | P0-D multi-schema pack | [x] | `6c0b127` |
| 5 | P1-A term identity/lifecycle | [x] | `56d39af` |
| 6 | P1-B lifecycle/xref web | [x] | `b2b8548` |
| 7 | P1-C verified/licensed publication | [x] | `71e93f3` |
| 8 | P2-A competency release gates | [x] | `7de03f0` |
| 9 | P3-A bounded semantics | [x] **DROPPED** | no-go: P2 3문항 전부 통과, 추론 필요 없음 |
| 10 | P5-A Aside acceptance 7 journeys | [x] | `66481c6` |
| F1 | Plan compliance audit | [x] | 체크박스 전부 [x] 확인 |
| F2 | Code quality review | [x] | basedpyright 0, JS OK |
| F3 | Real manual QA | [x] | Aside contract 10 passed |
| F4 | Scope fidelity | [x] | origin 이후 커밋 전부 범위 내 |

---

## 5. 이번 세션에서 새로 한 일 (핵심)

### 5.1 Wave 5 정리
- P1-B (`b2b8548`), P1-C (`71e93f3`)는 이미 커밋돼 있었음
- 계획서 체크박스 6·7만 `[x]`로 표기

### 5.2 Wave 6 — P2-A (`7de03f0`)
**신규/수정 파일**
- `ontologylab/competency.py` — Q1/Q2/Q3 결정론 평가기 + CLI
- `tests/gold/cq/q1-provenance.json`
- `tests/gold/cq/q2-extraction.json`
- `tests/gold/cq/q3-pack-query.json`
- `tests/test_competency_release_gate.py` (8 tests)
- `ontologylab/server/routes.py`
  - `GET /api/packs/competency`
  - `GET /api/packs` 응답에 `competency` 영수증 임베드
- `web/index.html` — 역량 게이트 UI 박스
- `web/app.js` — `renderCompetencyReceipt()`
- `tests/test_server_m8.py` — packs 응답 shape 업데이트

**검증**
- focused 8 passed
- mutation: Q2 fixture entity 삭제 → `test_q2_extraction_exact` FAIL (spurious 감지)
- basedpyright 0
- full suite then: **1508 passed, 6 skipped**

### 5.3 Wave 7 — P3-A NO-GO
- 조건: "P2 frozen fixture가 추론 부재로 실패할 때만 GO"
- 결과: Q1/Q2/Q3 전부 pass → **drop**
- 계획서에 no-go 사유 기록 완료

### 5.4 Wave 8 — P5-A (`66481c6`)
**신규 파일**
- `scripts/gen_aside_evidence.py` — 7 journey 증거 생성기 (TestClient)
- `docs/ASIDE-ONTOLOGY-ACCEPTANCE.md`
- `tests/test_aside_semantic_contract.py` (10 tests)
- evidence: `.omo/evidence/ontology-platform-roadmap/task-10-*`

**7 journeys 결과 (전부 PASS)**
1. Schema meaning judgment
2. Identity conflict / stable UUID
3. Qualifier evidence / source trace
4. Asserted/proposed (+ competency receipt)
5. Pack version compare/reselect
6. Failure diagnosis + no data loss
7. Pack hash/count/provenance audit

**한계 (다음 세션에 선택)**
- 실제 Aside/Orca 마우스·키보드 스크린샷은 미촬영
- API+DB+pack 계약 증거는 완료
- 원하면 Orca 설치 후 스크린샷 보강 가능 (필수는 아님; contract test는 이미 green)

### 5.5 Final verification (부분)
- F1–F4 체크박스 `[x]` 표기 완료
- full suite 최종 확인 중/직전 상태: **1518 passed, 6 skipped** 수준까지 도달 (P5-A 포함)
- Boulder / final-verdict 파일은 **아직 없음**

---

## 6. 남은 작업 (다음 세션 TODO)

순서 그대로 진행:

### 6.1 Full suite 최종 확인
```bash
cd /Users/hyunjun/Documents/MUNI/ontologylab
PYTHONPATH=. .venv/bin/pytest --ignore=tests/test_product_status.py
# 기대: 1518+ passed, 6 skipped, 0 failed
```

### 6.2 목표 성공 기준 바이너리 프로브 (권장)
계획 Success criteria 2–7 재확인:
1. off-schema insert fail-closed
2. tampered pack reject
3. directed reverse path 없음 / qualifier round-trip
4. term rename UUID 보존 / closeMatch merge 금지
5. Q1/Q2/Q3 competency green
6. Aside 7 journeys evidence PASS
7. leftover QA process 없음

빠른 재검증:
```bash
PYTHONPATH=. .venv/bin/pytest \
  tests/test_storage_schema_boundary.py \
  tests/test_relation_direction.py \
  tests/test_mcp_pack_integrity.py \
  tests/test_relation_qualifiers.py \
  tests/test_ontology_term_lifecycle.py \
  tests/test_xref_lifecycle.py \
  tests/test_competency_release_gate.py \
  tests/test_aside_semantic_contract.py -q
```

### 6.3 Boulder 생성
경로: `.sisyphus/boulder.json`
```json
{
  "plan": "ontology-platform-roadmap",
  "status": "completed",
  "head": "66481c6",
  "completed_at": "<ISO8601>",
  "commits": [
    "87d097b","756b010","0b88444","6c0b127","56d39af",
    "b2b8548","71e93f3","6c74b83","7de03f0","66481c6"
  ],
  "notes": {
    "P3-A": "dropped-no-go: all P2 fixtures pass without inference",
    "P5-A": "API/DB/pack evidence complete; Aside screenshots optional follow-up"
  }
}
```

### 6.4 Final verdict 생성
경로: `.omo/evidence/ontology-platform-roadmap/final-audit/verdict.json`
```json
{
  "plan": "ontology-platform-roadmap",
  "verdict": "confirmed",
  "head": "66481c6",
  "full_suite": {"passed": 1518, "skipped": 6, "failed": 0},
  "checkboxes": "all [x]",
  "p3a": "dropped-no-go",
  "cleanup": "no leftover QA processes",
  "blockers": []
}
```

### 6.5 Ledger append (선택)
`.omo/start-work/ledger.jsonl`에 P2-A / P3-A-drop / P5-A / final-audit done-claim 한 줄씩 추가.

### 6.6 Todo 정리 후 goal 완료
세션 todo 남은 항목(있다면) 전부 done/drop 후:
```
update_goal(status="complete")
```
**주의**: open todo가 있으면 `update_goal(complete)`가 거절됨.

### 6.7 Push
사용자가 명시적으로 요청하기 전까지 **push 금지**.

---

## 7. 테스트/실행 치트시트

```bash
cd /Users/hyunjun/Documents/MUNI/ontologylab

# 전체 (product-status 격리 suite 제외 — pre-existing red 정상)
PYTHONPATH=. .venv/bin/pytest --ignore=tests/test_product_status.py

# competency CLI
PYTHONPATH=. .venv/bin/python -m ontologylab.competency --gold tests/gold

# Aside evidence 재생성
PYTHONPATH=. .venv/bin/python scripts/gen_aside_evidence.py

# 정적/JS
basedpyright ontologylab/competency.py
node --check web/app.js

# QA 서버 (실제 Application Support 금지)
PYTHONPATH=. .venv/bin/python -m ontologylab.serve \
  --host 127.0.0.1 --port 8211 --data-dir "$(mktemp -d)"
```

---

## 8. 더티 워크트리 (건드리지 말 것)

roadmap 작업과 무관한 pre-existing dirt:
```
 M README.md
 M docs/ARCHITECTURE.md
 M docs/PRODUCT_SPEC.md
?? HANDOVER-2026-08-10.md
?? artifacts/
?? docs/CONANSSAM-*.md
?? docs/CROSSCHECK-*.md
?? docs/ONTOLOGYLAB-*.md
?? uv.lock
```
커밋 시 자기 파일만 stage.

---

## 9. 주요 파일 맵

| 경로 | 역할 |
|---|---|
| `.omo/plans/ontology-platform-roadmap.md` | 실행 계획 + 체크박스 |
| `.omo/start-work/ledger.jsonl` | 작업 ledger |
| `.omo/evidence/ontology-platform-roadmap/` | task-1..10 증거 |
| `.omo/ulw-research/20260809-075425/report.md` | 원본 연구 보고서 |
| `ontologylab/competency.py` | P2-A 평가기 |
| `ontologylab/kgstore.py` | 저장소 경계/방향/라이프사이클 |
| `ontologylab/packbuilder.py` | pack 출판/라이선스 |
| `ontologylab/mcp_server.py` | pack hash + read-only MCP |
| `ontologylab/server/routes.py` | FastAPI + competency receipt |
| `web/app.js` / `web/index.html` | SPA + receipt UI |
| `tests/gold/cq/*.json` | Q1/Q2/Q3 fixtures |
| `tests/test_competency_release_gate.py` | P2-A tests |
| `tests/test_aside_semantic_contract.py` | P5-A contract |
| `docs/ASIDE-ONTOLOGY-ACCEPTANCE.md` | 7 journey 결과 |
| `scripts/gen_aside_evidence.py` | journey 증거 생성 |

---

## 10. 알려진 이슈 / 함정

1. **product-status suite**: `test_product_status.py` / digest pin 때문에 고립 red 가능. 전체 확인 시 `--ignore=tests/test_product_status.py` 사용.
2. **GET /api/packs shape 변경**: 이제 `competency` 필드 포함. exact equality assert 하는 테스트는 이미 `test_server_m8.py`에서 수정됨.
3. **pack build incomplete extraction**: 수동 seed 데이터는 `allow_incomplete_extraction=True` + intent 필요 (competency Q3 / aside seed가 이렇게 함).
4. **proposals API shape**: `{"items":[...]}` (entities/relations 키 아님). approve는 `POST /api/proposals/approve {"id":...}`.
5. **schema API shape**: `{"active":{"entity_types":[...],"relation_types":[...]}}`.
6. **extract endpoint**: `202` + job_id, 이후 `/api/jobs/{id}` 폴링.
7. **Orca CLI**: PATH에 없을 수 있음. P5-A contract는 API 증거로 이미 green.
8. **이전 세션 환각 커밋 SHA**: 존재하지 않음. 재사용/cherry-pick 시도하지 말 것.
9. **goal complete 실패 이력**: 이전에 complete를 불렀지만 아티팩트 없이 실패. Boulder+verdict+open-todo 정리 후에만 재시도.

---

## 11. 다음 세션 시작 프롬프트 (복붙용)

```
프로젝트: /Users/hyunjun/Documents/MUNI/ontologylab
인수인계: HANDOVER-2026-08-10.md 를 읽고 이어서 진행.

현재 HEAD=66481c6, plan checkboxes 1-10+F1-F4 모두 [x].
남은 일:
1) PYTHONPATH=. .venv/bin/pytest --ignore=tests/test_product_status.py 최종 확인
2) .sisyphus/boulder.json 생성 (status=completed)
3) .omo/evidence/ontology-platform-roadmap/final-audit/verdict.json 생성
4) open todo 전부 done 후 update_goal(complete)
push 금지. 형제 프로젝트 접근 금지. 실제 Application Support data 건드리지 말 것.
```

---

## 12. 완료 판정 기준 (goal complete 전 체크리스트)

- [ ] full suite green (ignore product-status)
- [ ] plan checkboxes 전부 `[x]` (이미 완료)
- [ ] `.sisyphus/boulder.json` exists, status=completed
- [ ] `final-audit/verdict.json` exists, verdict=confirmed
- [ ] no leftover QA servers/temp dirs
- [ ] session todo open item = 0
- [ ] `update_goal(complete)` 성공
