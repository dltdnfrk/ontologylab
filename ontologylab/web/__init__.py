"""Dashboard static resources for the local web layer.

Data-only package: the HTML document, the vanilla-JS modules, the
stylesheet, and the Pretendard font, plus ``manifest.json`` pinning every
servable file by path, size, and SHA-256. Access and verification go
through :mod:`ontologylab.web_assets` — never by guessing filesystem paths.
"""
